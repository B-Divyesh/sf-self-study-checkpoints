[
  {
    "id": "workspace-planning",
    "claim": "Builds a 42–84 day checkpoint with linked problems, success criteria, rubric, reviewer, and evidence",
    "where": "README feature list, builder",
    "test": "npm run test:e2e -- --grep @claim:workspace-planning --project=desktop",
    "sandbox": "fresh browser context; complete every owner planning step with one linked proof problem and evidence"
  },
  {
    "id": "human-review",
    "claim": "Exports a human review request and imports the reviewer response after checksum verification",
    "where": "README feature list, review and packet screens",
    "test": "npm run test:e2e -- --grep @claim:human-review --project=desktop",
    "sandbox": "fresh browser context; owner exports request; separate reviewer page scores rubric and downloads response; owner imports it"
  },
  {
    "id": "sealed-packet",
    "claim": "Exports a completion packet with an ECDSA P-256 integrity seal that can be verified in the browser",
    "where": "landing page, README",
    "test": "npm run test:e2e -- --grep @claim:sealed-packet --project=desktop",
    "sandbox": "fresh browser context; complete owner and reviewer sample flow; export packet and verify its seal"
  },
  {
    "id": "free-no-account",
    "claim": "The product is free to use and requires no account",
    "where": "landing facts, README",
    "test": "npm run test:e2e -- --grep @claim:free-no-account --project=desktop",
    "sandbox": "open the one-click demo without credentials; edit and navigate it while recording requests; assert no auth or payment origin is contacted"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "README, service worker behavior",
    "test": "npm run test:e2e -- --grep @claim:offline-reload --project=desktop",
    "sandbox": "dedicated fresh browser context; visit demo; wait for service-worker control; set offline; reload"
  },
  {
    "id": "demo-sandbox",
    "claim": "Demo sample changes are temporary and never overwrite real checkpoint storage",
    "where": "demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox --project=desktop",
    "sandbox": "seed a sentinel in the real storage key; edit and reset demo; assert the sentinel is unchanged and no demo keys exist"
  },
  {
    "id": "local-only",
    "claim": "The app sends no checkpoint or demo data to another origin",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-only --project=desktop",
    "sandbox": "record every browser request while editing and navigating the sample checkpoint; assert every request is same-origin"
  }
]
